import React from 'react';

const DesignerPanel = ({ isDarkMode }) => {
  return (
    <div>
      <h2 className="text-lg font-semibold mb-2">Designer</h2>
      <p>Design your simulation here.</p>
      {/* Add more designer controls here */}
    </div>
  );
};

export default DesignerPanel;
