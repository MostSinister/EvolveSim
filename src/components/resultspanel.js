import React from 'react';

const ResultsPanel = ({ isDarkMode }) => {
  return (
    <div>
      <h2 className="text-lg font-semibold mb-2">Results</h2>
      <p>Simulation results will appear here.</p>
      {/* Add more results display here */}
    </div>
  );
};

export default ResultsPanel;
