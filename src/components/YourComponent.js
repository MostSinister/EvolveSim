// Assuming you have a component for the select fields
function YourComponent({ isDarkMode }) {
    return (
        <div>
            <select className="select-component-type">
                {/* Options */}
            </select>
            <select className="sort-by">
                {/* Options */}
            </select>
            <div className="component-entry">
                {/* Component content */}
            </div>
        </div>
    );
}
